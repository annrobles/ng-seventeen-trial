import { AppMaskPostcodeDirective } from './app-mask-postcode.directive';

describe('AppMaskPostcodeDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMaskPostcodeDirective();
    expect(directive).toBeTruthy();
  });
});
