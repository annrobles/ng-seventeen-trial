import { AppMaskPhoneDirective } from './app-mask-phone.directive';

describe('AppMaskPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMaskPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
